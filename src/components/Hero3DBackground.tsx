import { Canvas } from "@react-three/fiber";

export const Hero3DBackground = () => {
  return (
    <div className="pointer-events-none absolute inset-0 z-0">
      {/* Simple transparent background - no 3D elements */}
    </div>
  );
};

export default Hero3DBackground;
